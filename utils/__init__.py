# i notebooks, lägg till import sys, import os
# Lägg till projektets root i sys.path (en gång per notebook)
# sys.path.append(os.path.abspath(os.path.join(os.getcwd(), "..", "..")))
# "..", antal steg från notebook ner till root

# importera funktionerna
# from utils.gemini_func import fix_prompt, ask_gemini
# from utils.predictions_models import eda, predict
